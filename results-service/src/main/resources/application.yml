server:
  port: 8083

spring:
  application:
    name: results-service
  datasource:
    url: jdbc:postgresql://localhost:5434/resultsdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
